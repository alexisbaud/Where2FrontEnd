/**
 * Constantes d'espacement pour l'application Where2
 */

const spacing = {
  // Espacements de base
  xs: 4,
  sm: 8,
  md: 16,
  lg: 24,
  xl: 32,
  xxl: 48,
  
  // Espacements spécifiques
  screenPadding: 16,   // Padding horizontal des écrans
  sectionMargin: 24,   // Marge entre les sections
  buttonPadding: 16,   // Padding interne des boutons
  inputPadding: 12,    // Padding interne des inputs
  borderRadius: 8,     // Rayon de bordure standard
};

export default spacing; 